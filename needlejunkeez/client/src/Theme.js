import { createTheme } from '@mui/material/styles';

const theme = createTheme({
  palette: {
    background: {
      default: '#d3d4d9', // Replace with your desired background color
    },
  },
});

export default theme;